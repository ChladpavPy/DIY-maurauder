# DIY-marauder
custom built marauder pcb based on rp2040 microcontroller 
while rp2040 uses the same positions for pins as esp32 (wroom version would be needed for our project, or you can use esp32 s3), you can just switch esp32 for rp2040 on the board which would suit our project
